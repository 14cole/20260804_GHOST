# Reviewed GRIM internal-team dependency lock.
# Target: 64-bit CPython 3.12 on Windows.
# Core versions below are the exact stack exercised by the release gate.
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.2
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
PySide6==6.11.2
PySide6_Addons==6.11.2
PySide6_Essentials==6.11.2
python-dateutil==2.9.0.post0
scipy==1.18.1
shiboken6==6.11.2
six==1.17.0

# Optional internal-team capabilities. They remain optional at runtime, but a
# machine that installs them must use these reviewed versions.
psutil==7.0.0
pywin32==311; sys_platform == "win32"

# Offline editable installation needs a local build backend. These are put in
# the team wheelhouse so pip never needs to resolve build requirements online.
setuptools==80.9.0
wheel==0.45.1
